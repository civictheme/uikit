{#
/**
 * @file
 * CivicTheme My Slider component.
 *
 * Props:
 * - theme: [string] Theme variation (light or dark).
 * - simple_prop: [string] A simple string property.
 * - complex_object: [object] A complex object property.
 *   Each property contains:
 *   - text: [string] Object text.
 *   - url: [string] Object URL.
 *   - is_active: [boolean] Whether the object is active.
 * - array_prop: [array] An array property.
 *   Each item contains:
 *   - items [string]
 * - nested_array: [array] A nested array property:
 *   Each item contains:
 *   - id: [string] Item ID.
 *   - value: [string] Item value.
 *
 * Slots:
 * - title: Component title.
 * - content: Component content.
 * - footer: Component footer.
 *
 * Blocks:
 * - header_block
 * - content_block
 * - array_items_block
 * - nested_array_block
 * - footer_block
 */
#}

<div class="test-component">
  {% block header_block %}
    <div class="test-component__header">
      {% if title %}
        <h2>{{ title }}</h2>
      {% endif %}
    </div>
  {% endblock %}

  {% block content_block %}
    <div class="test-component__content">
      {% if content %}
        {{ content|raw }}
      {% endif %}

      {% if complex_object %}
        <a href="{{ complex_object.url }}" class="{{ complex_object.is_active ? 'active' : '' }}">
          {{ complex_object.text }}
        </a>
      {% endif %}
    </div>
  {% endblock %}

  {%- block array_items_block -%}
    {% if array_prop is not empty %}
      <ul class="test-component__items">
        {% for item in array_prop %}
          <li>{{ item }}</li>
        {% endfor %}
      </ul>
    {% endif %}
  {% endblock %}

  {% block nested_array_block -%}
    {% if nested_array is not empty %}
      <div class="test-component__nested">
        {% for item in nested_array %}
          <div id="{{ item.id }}">{{ item.value }}</div>
        {% endfor %}
      </div>
    {% endif %}
  {% endblock %}

  {%- block footer_block %}
    {% if footer %}
      <div class="test-component__footer">
        {{ footer|raw }}
      </div>
    {% endif %}
  {% endblock %}
</div>
