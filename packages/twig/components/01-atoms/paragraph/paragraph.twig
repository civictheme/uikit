{#
/**
 * @file
 * CivicTheme Paragraph component.
 *
 * Variables:
 * - theme: [string] Theme variation (light or dark).
 * - content: [string] Paragraph content.
 * - size: [string] Paragraph size (extra-large, large, regular, small).
 * - allow_html: [boolean] Whether to allow HTML in content.
 * - attributes: [string] Additional HTML attributes.
 * - modifier_class: [string] Additional CSS classes.
 */
#}

{% set size_class = 'ct-paragraph--%s'|format(size|default('regular')) %}
{% set theme_class = 'ct-theme-%s'|format(theme|default('light')) %}
{% set modifier_class = '%s %s %s'|format(theme_class, size_class, modifier_class|default('')) %}

{% if content -%}
  <div class="ct-paragraph {{ modifier_class -}}" {% if attributes %}{{- attributes|raw -}}{% endif %}>
    {% if allow_html ? allow_html : false %}
      {{- content|raw -}}
    {% else %}
      {{- content -}}
    {% endif %}
  </div>
{%- endif %}
