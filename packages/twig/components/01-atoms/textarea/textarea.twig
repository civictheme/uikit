{#
/**
 * @file
 * CivicTheme Textarea component.
 *
 * Props:
 * - theme: [string,null,integer] Theme variation (light or dark).
 * - name: [string,null,integer] DOM name (required).
 * - id: [string,null,integer] DOM ID attribute.
 * - value: [string,null,integer] The textarea value.
 * - placeholder: [string,null,integer] Placeholder text.
 * - rows: [integer,string,null] Display rows count.
 * - is_invalid: [boolean,null,string,integer] Whether the textarea is invalid.
 * - is_disabled: [boolean,null,string,integer] Whether the textarea is disabled.
 * - is_required: [boolean,null,string,integer] Whether the textarea is required.
 * - modifier_class: [string,null,integer] Additional CSS classes.
 */
#}

{% set theme_class = 'ct-theme-%s'|format(theme|default('light')) %}
{% set is_invalid_class = is_invalid ? 'ct-textarea--is-invalid' : '' %}
{% set modifier_class = '%s %s %s'|format(theme_class, is_invalid_class, modifier_class|default(''))|trim %}

{% if name is not empty %}
  <textarea
    class="ct-textarea {{ modifier_class -}}"
    name="{{ name }}"
    {% if rows %}rows="{{ rows }}"{% endif %}
    {% if id %}id="{{ id }}"{% endif %}
    {% if is_disabled %}disabled{% endif %}
    {% if is_required %}required{% endif %}
    {% if is_invalid %}aria-invalid="true"{% endif %}
    {% if placeholder %}placeholder="{{ placeholder }}"{% endif %}
    {% if attributes is defined and attributes is not null %}{{- attributes -}}{% endif %}
  />{{ value|default('') }}</textarea>
{% endif %}
