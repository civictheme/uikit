{#
/**
 * @file
 * CivicTheme Radio component.
 *
 * Props:
 * - theme: [string,null,integer] Theme variation (light or dark).
 * - name: [string,null,integer] DOM name (required).
 * - id: [string,null,integer] DOM id (required).
 * - value: [string,null,integer] DOM value.
 * - label: [string,null,integer] Label content.
 * - is_checked: [boolean,null,string,integer] Whether the radio is checked.
 * - is_required: [boolean,null,string,integer] Whether the radio is required.
 * - is_invalid: [boolean,null,string,integer] Whether the radio is invalid.
 * - is_disabled: [boolean,null,string,integer] Whether the radio is disabled.
 * - modifier_class: [string,null,integer] Additional CSS classes.
 */
#}

{% set theme_class = 'ct-theme-%s'|format(theme|default('light')) %}
{% set is_invalid_class = is_invalid ? 'ct-radio--is-invalid' : '' %}
{% set modifier_class = '%s %s %s'|format(theme_class, is_invalid_class, modifier_class|default(''))|trim %}

{% if name is not empty and id is not empty %}
  <input
    type="radio"
    class="ct-radio {{ modifier_class -}}"
    name="{{ name }}"
    value="{{ value }}"
    id="{{ id }}"
    {% if is_checked %}checked{% endif %}
    {% if is_required %}required{% endif %}
    {% if is_disabled %}disabled{% endif %}
    {% if is_invalid %}aria-invalid="true"{% endif %}
    {% if attributes is defined and attributes is not null %}{{- attributes -}}{% endif %}
  />
  {% if label is not empty %}
    {% include '@atoms/label/label.twig' with {
      theme: theme,
      content: label,
      for: id,
      size: 'small',
      modifier_class: 'ct-radio__label',
    } only %}
  {% endif %}
{% endif %}
