{#
/**
 * @file
 * CivicTheme Label component.
 *
 * Props:
 * - theme: [string,null,integer] Theme variation (light or dark).
 * - tag: [string,null,integer] HTML tag to use (label, legend).
 * - content: [string,null,integer] The label's content.
 * - size: [string,null,integer] Label size (extra-large, large, regular, small, extra-small).
 * - is_required: [boolean,null,string,integer] Whether the label is required.
 * - required_text: [string,null,integer] Text to display within label element when required.
 * - for: [string,null,integer] Form element ID this label belongs to.
 * - modifier_class: [string,null,integer] Additional CSS classes.
 */
#}

{% set tag = tag in ['label', 'legend'] ? tag : 'label' %}
{% set size = size in ['extra-large', 'large', 'regular', 'small', 'extra-small'] ? size : 'regular' %}
{% set size_class = 'ct-label--%s'|format(size) %}
{% set required_class = is_required ? 'ct-label--required' : '' %}
{% set required_text = required_text|default('(required)') %}
{% set theme_class = 'ct-theme-%s'|format(theme|default('light')) %}
{% set modifier_class = '%s %s %s %s'|format(theme_class, size_class, required_class, modifier_class|default('')) %}

{% if content is not empty -%}
  <{{ tag }}
  class="ct-label {{ modifier_class -}}"
  {% if for is not empty %} for="{{ for }}" {% endif %}
  {% if attributes is defined and attributes is not null %}{{- attributes -}}{% endif %}
  >
  {{- content -}}{%- if is_required %} <span class="ct-label__required">{{ required_text }}</span>{%- endif -%}
  </{{ tag }}>
{%- endif %}
