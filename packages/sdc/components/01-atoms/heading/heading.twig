{#
/**
 * @file
 * CivicTheme Heading component.
 *
 * Props:
 * - theme: [string,null,integer] Theme variation (light or dark).
 * - content: [string,null,integer] Heading content.
 * - level: [string,null,integer] Heading level (1-6).
 * - modifier_class: [string,null,integer] Additional CSS classes.
 */
#}

{% set theme_class = 'ct-theme-%s'|format(theme|default('light')) %}
{% set modifier_class = '%s %s'|format(theme_class, modifier_class|default('')) %}

{% set level = level|default('2') %}

{% if content %}
  <h{{ level }} class="ct-heading {{ modifier_class -}}" {% if attributes is defined and attributes is not null %}{{- attributes -}}{% endif %}>
    {{- content -}}
  </h{{ level }}>
{% endif %}
