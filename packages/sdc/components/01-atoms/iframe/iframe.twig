{#
/**
 * @file
 * CivicTheme Iframe component.
 *
 * Props:
 * - theme: [string,null,integer] Theme variation (light or dark).
 * - url: [string,null,integer] Iframe URL.
 * - width: [integer,string,null] Iframe width.
 * - height: [integer,string,null] Iframe height.
 * - vertical_spacing: [string,null,integer] Vertical spacing position (top, bottom, both).
 * - with_background: [boolean,null,string,integer] Whether to include a background.
 * - modifier_class: [string,null,integer] Additional CSS classes.
 */
#}

{% set with_background_class = with_background ? 'ct-iframe--with-background' : '' %}
{% set vertical_spacing_class = vertical_spacing in ['top', 'bottom', 'both'] ? 'ct-vertical-spacing-inset--%s'|format(vertical_spacing) : '' %}
{% set theme_class = 'ct-theme-%s'|format(theme|default('light')) %}
{% set modifier_class = '%s %s %s %s'|format(theme_class, vertical_spacing_class, with_background_class, modifier_class|default('')) %}

<iframe
  class="ct-iframe {{ modifier_class -}}"
  {% if url %}src="{{ url }}"{% endif %}
  {% if width %}width="{{ width }}"{% endif %}
  {% if height %}height="{{ height }}"{% endif %}
  {% if attributes is defined and attributes is not null %}{{- attributes -}}{% endif %}
></iframe>
